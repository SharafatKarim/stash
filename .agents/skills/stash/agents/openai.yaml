interface:
  display_name: "Stash"
  short_description: "Repo-specific patterns and workflows for stash"
  default_prompt: "Use the stash repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true